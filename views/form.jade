doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body 
    form(method="POST" action="/new")
        input(name="nameinput")
        input(name="messageinput")
        button(name="submitbutton") Create post!
